import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'learn-ionic-vue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
